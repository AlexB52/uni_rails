#!/usr/bin/env bash

bundle install
bundle exec rake build
ls -t pkg | head -n1 | xargs -I {} mv pkg/{} test/features/todos-scaffold/uni_rails.gem
docker-compose -f test/features/todos-scaffold/docker-compose.yml up --build --exit-code-from test